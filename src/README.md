# Creative Automation Pipeline (ADOBE POC)

A scalable Proof of Concept for automating social ad asset generation using GenAI.
## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Add OpenAI Key to `.env` file.
3. Run the pipeline: 
   ```bash
   python -m src.main --brief brief.json